#
# Board configuration for OpenClaw Cuttlefish (arm64)
#
# Inherits from standard AOSP cuttlefish ARM64 board.
#

# Reference the base cuttlefish board config
# This will be relative when used in AOSP build
-include device/google/cuttlefish/vsoc_arm64/BoardConfig.mk

# OpenClaw-specific board configurations (if any) go here
# For now, we use the standard AOSP Cuttlefish configuration as-is.
